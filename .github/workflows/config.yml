name: Tests

on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: Install Docker Compose
        run: |
          curl -L "https://github.com/docker/compose/releases/download/v2.20.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          chmod +x /usr/local/bin/docker-compose

      - name: Run test via docker compose
        id: run-tests
        run: |
          start_time=$(date +%s)  # Фиксируем время начала
          docker-compose up --exit-code-from regression
          end_time=$(date +%s)    # Фиксируем время окончания
          duration=$((end_time - start_time))
          echo "DURATION_SEC=$duration" >> $GITHUB_OUTPUT
          echo "DURATION_READABLE=$(printf '%02d:%02d:%02d' $((duration/3600)) $(( (duration%3600)/60 )) $((duration%60)))" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Generate Allure report with history
        if: always()
        run: |
          # Создаём необходимые директории
          mkdir -p allure-report/history
          mkdir -p allure-results/history

          # Копируем историю из предыдущего отчёта (если существует)
          if [ -d "allure-report/history" ]; then
            cp -r allure-report/history/* allure-results/history/ || true
          fi

          # Генерируем новый отчёт с сохранением истории
          docker-compose run regression /bin/sh -c "allure generate allure-results --clean -o allure-report"

          # Создаём резервный summary.json если его нет
          mkdir -p allure-results/widgets
          if [ ! -f allure-results/widgets/summary.json ]; then
            echo '{"statistic":{"total":0,"passed":0,"failed":0,"skipped":0}}' > allure-results/widgets/summary.json
          fi

          # Сохраняем историю для следующего запуска
          cp -r allure-report/history allure-results/ || true

          # Добавляем информацию о среде выполнения
          echo "RUN_NUMBER=${{ github.run_number }}" > allure-results/environment.properties
          echo "GIT_BRANCH=${{ github.ref }}" >> allure-results/environment.properties
          
      - name: Parse Allure results
        if: always()
        id: allure-stats
        run: |
          if ! command -v jq &> /dev/null; then
            sudo apt-get update && sudo apt-get install -y jq
          fi

          # Создаем файл с дефолтными значениями если его нет
          if [ ! -f "allure-report/widgets/summary.json" ]; then
            mkdir -p allure-report/widgets
            echo '{"statistic":{"total":0,"passed":0,"failed":0,"skipped":0,"broken":0,"canceled":0}}' > allure-report/widgets/summary.json
          fi

          # Парсим с дефолтными значениями
          TOTAL=$(jq '.statistic.total // 0' allure-report/widgets/summary.json)
          PASSED=$(jq '.statistic.passed // 0' allure-report/widgets/summary.json)
          FAILED=$(jq '.statistic.failed // 0' allure-report/widgets/summary.json)
          SKIPPED=$(jq '.statistic.skipped // 0' allure-report/widgets/summary.json)
          BROKEN=$(jq '.statistic.broken // 0' allure-report/widgets/summary.json)
          CANCELED=$(jq '.statistic.canceled // 0' allure-report/widgets/summary.json)

          # Рассчитываем проценты
          if [ $TOTAL -gt 0 ]; then
            PASSED_PERCENT=$(( ($PASSED * 100) / $TOTAL ))
            FAILED_PERCENT=$(( ($FAILED * 100) / $TOTAL ))
            SKIPPED_PERCENT=$(( ($SKIPPED * 100) / $TOTAL ))
            BROKEN_PERCENT=$(( ($BROKEN + 100) / $TOTAL ))
          else
            PASSED_PERCENT=0
            FAILED_PERCENT=0
            SKIPPED_PERCENT=0
            BROKEN_PERCENT=0
          fi

          echo "TOTAL=$TOTAL" >> $GITHUB_OUTPUT
          echo "PASSED=$PASSED" >> $GITHUB_OUTPUT
          echo "FAILED=$FAILED" >> $GITHUB_OUTPUT
          echo "SKIPPED=$SKIPPED" >> $GITHUB_OUTPUT
          echo "BROKEN=$BROKEN" >> $GITHUB_OUTPUT
          echo "CANCELED=$CANCELED" >> $GITHUB_OUTPUT
          echo "PASSED_PERCENT=$PASSED_PERCENT" >> $GITHUB_OUTPUT
          echo "FAILED_PERCENT=$FAILED_PERCENT" >> $GITHUB_OUTPUT
          echo "SKIPPED_PERCENT=$SKIPPED_PERCENT" >> $GITHUB_OUTPUT
          echo "BROKEN_PERCENT=$BROKEN_PERCENT" >> $GITHUB_OUTPUT
          

      - name: Upload Allure report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@4.1.5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages
          folder: allure-report
          clean: false

      - name: Determine final status
        id: final-status
        run: |
          # Проверяем наличие упавших тестов
          if [ "${{ steps.allure-stats.outputs.FAILED }}" -gt 0 ] || [ "${{ steps.allure-stats.outputs.BROKEN }}" -gt 0 ]; then
            echo "FINAL_STATUS=❌ ОШИБКИ" >> $GITHUB_OUTPUT
            echo "FINAL_MESSAGE=⚠️ <b>Требуется проверка упавших тестов!</b>" >> $GITHUB_OUTPUT
          else
            echo "FINAL_STATUS=✅ УСПЕШНО" >> $GITHUB_OUTPUT
            echo "FINAL_MESSAGE=🎉 <b>Все тесты прошли успешно!</b>" >> $GITHUB_OUTPUT
          fi

      - name: Send Telegram notification
        if: always()
        uses: appleboy/telegram-action@v1.0.0
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          message: |
            <b>📊 Результаты тестов</b>
            
            
            <b>Проект:</b> ${{ github.repository }}
            <b>Ветка:</b> ${{ github.ref_name }}
            <b>Статус:</b> ${{ steps.allure-stats.outputs.FAILED == '0' && '✅' || '❌' }}
            <b>Время выполнения:</b> ${{ steps.run-tests.outputs.DURATION_READABLE }}
            
            
            <b>📈 Статистика</b> (${{ steps.allure-stats.outputs.TOTAL }} тестов):
            <code>│</code> • ✅ <b>Успешных:</b> ${{ steps.allure-stats.outputs.PASSED }} (${{ steps.allure-stats.outputs.PASSED_PERCENT }}%)
            <code>│</code> • ❌ <b>Упавших:</b> ${{ steps.allure-stats.outputs.FAILED }} (${{ steps.allure-stats.outputs.FAILED_PERCENT }}%)
            <code>│</code> • ⏩ <b>Пропущенных:</b> ${{ steps.allure-stats.outputs.SKIPPED }} (${{ steps.allure-stats.outputs.SKIPPED_PERCENT }}%)
            ${{ steps.allure-stats.outputs.BROKEN != '0' && '<code>│</code> • 🛠 <b>Сломанных:</b> ' || '' }}${{ steps.allure-stats.outputs.BROKEN != '0' && format('{0} ({1}%)', steps.allure-stats.outputs.BROKEN, steps.allure-stats.outputs.BROKEN_PERCENT) || '' }}            
            
            
            <b>🔗 Ссылки</b>:
            <code>│</code> • <a href="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/">Allure Report</a>
            <code>│</code> • <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}">GitHub Actions</a>
            
            
            ${{ steps.final-status.outputs.FINAL_MESSAGE }}
          format: html
          disable_web_page_preview: true